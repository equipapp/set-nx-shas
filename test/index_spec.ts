// Expect style
import 'mocha';
import { expect } from 'chai';

describe('index.ts', function() {
  // test a functionality
  it('should add numbers', function() {
    // add an assertion
    expect(1).to.equal(1);
  })
})
